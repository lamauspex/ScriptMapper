# 🎬 Инструменты для обработки сценариев

## Созданные файлы:

### Основные файлы:
- **`script_loader.py`** - основной модуль обработки сценариев
- **`run_processing.py`** - запуск обработки 
- **`quick_test.py`** - быстрый тест функциональности

### Примеры и документация:
- **`example_usage.py`** - базовые примеры использования
- **`advanced_usage.py`** - продвинутые возможности
- **`cli_tool.py`** - инструмент командной строки
- **`README_processing.md`** - подробная инструкция

## Быстрый старт:

### 1. Проверка работоспособности:
```bash
python quick_test.py
```

### 2. Полная обработка сценария:
```bash
python run_processing.py
```

### 3. Информация о файле:
```bash
python quick_test.py info
```

## Что делает каждый файл:

| Файл | Назначение | Когда использовать |
|------|------------|-------------------|
| `script_loader.py` | Основной модуль | Всегда загружается автоматически |
| `run_processing.py` | Обработка "_ 1 СЕРИЯ_.pdf" | Для тестирования конкретного файла |
| `quick_test.py` | Быстрая проверка | Перед основной обработкой |
| `example_usage.py` | Примеры кода | Изучение возможностей |
| `advanced_usage.py` | Продвинутые функции | Пакетная обработка |
| `cli_tool.py` | Командная строка | Автоматизация через терминал |

## Рекомендуемый порядок действий:

1. **Сначала запустите тест:**
   ```bash
   python quick_test.py
   ```

2. **Если тест прошел успешно, запустите обработку:**
   ```bash
   python run_processing.py
   ```

3. **Просмотрите результаты** в созданных файлах:
   - `_ 1 СЕРИЯ__processed.json` - данные
   - `_ 1 СЕРИЯ__analysis.txt` - анализ

## Возможные настройки:

Если нужно изменить паттерны распознавания сцен, отредактируйте `scene_patterns` в `ScriptLoader()`.

## Поддерживаемые форматы:
- ✅ PDF файлы (.pdf)
- ✅ DOCX файлы (.docx)

## Что получаем на выходе:
- Структурированные данные в JSON
- Читаемый анализ в текстовом файле
- Статистику по сценам и локациям
- Метаданные каждой сцены